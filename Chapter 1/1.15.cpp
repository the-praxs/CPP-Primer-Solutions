/*
    Write programs that contain the common errors discussed in the box on 
    page 16. Familiarize yourself with the messages the compiler generates.
*/

#include <iostream>

int main() {
    int v1 = 0, v2 = 10     // Syntax error: Missing a ;

    std :: cout << "Sum = " << v + v2;      // Declaration error: v does not exist 
    
    return 0;
}